<div class="prose mx-auto">
  <div class="flex justify-between items-center mb-8 gap-4">
    <h1 class="mb-0"><%= @license.name %></h1>
    <%= link_to "View on GitHub", @license.product.github_repo, target: :_blank, class: "flex-shrink-0 bg-black text-white no-underline font-bold px-4 py-2 rounded" if @license.users_count? %>
  </div>

  <%= render @license.users %>
  <%= render "license/users/form", license: @license if @license.available? %>
</div>
