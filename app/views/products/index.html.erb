<div class="grid grid-cols-3 gap-6">
  <%= render @products %>
  <%= link_to "+ Add a product", new_madmin_product_path, class: "border dashed border-gray-200 rounded-lg flex items-center justify-center font-semibold hover:bg-gray-50" if current_user&.admin? %>
</div>
